\section{Metodología}
\subsection{Propuesta de solución}
\label{propuesta_solucion}
La solución propuesta consiste en desarrollar un sistema web desde cero siguiendo la arquitectura MVC con el framework ASP.NET Core (.NET 10), con persistencia de datos y almacenamiento en la nube. El sistema consta de dos módulos, el módulo de conversion y carga de archivos, que permite subir documentos en distintos formatos para su conversión a PDF, asi como imágenes y enlaces. Y el módulo de gestión documental, que permite ver todos los archivos almacenados en el sistema y generar codigos QR para cada uno de ellos, asi como la visualización, la eliminación o la descarga de los archivos, todo desde una misma plataforma.

El proceso de autenticación se integra con el mecanismo existente en la institucion, ya que esta plataforma solo sera accedida como una extensión de sistemas que ya tienen su proceso de autenticación.

\subsubsection{Beneficios del sistema propuesto}
Al implementar esta propuesta la gestión documental del PJETAM mejoraría en la accesibilidad de los documentos, sería más fácil el referenciar los documentos mediante su código QR ya sea digital o impreso, el sistema también facilita el proceso de almacenar los documentos en un mismo sitio, y hace los procedimientos más fáciles al hacerlo en la misma plataforma, quitando la necesidad de usar herramientas externas.
%
\subsection{Análisis del sistema}
Antes de iniciar a diseñar el sistema se deben de tener claro los requerimientos del sistema, saber el problema del usuario y como resolverlo, que debe de hacer el sistema para satisfacer las necesidades del sistema. Para planear el flujo del sistema se necesita visualizar las acciones que hará cada usuario, en este sistema solo hay un tipo de usuario y sus acciones se ilustran en la Figura \ref{fig:caso-uso}

\subsubsection{Diagrama de caso de uso}
Antes de tener el proceso de autenticación claro, definí las acciones que iba a ofrecer el sistema y los usuarios que lo van a utilizar.
\begin{figure}[!tbh]
    \centering
    \includegraphics[width=0.7\linewidth]{img/caso de uso- diagrama.png}
    \caption{Diagrama caso de uso del sistema. Fuente: elaboración propia}
    \label{fig:caso-uso}
\end{figure}

\subsubsection{Diagramas de flujo del sistema}
Para entender como interactuan las acciones del usuario y los componentes del sistema, es necesario usar diagramas de flujo, como los que se presentan a continuación, que muestra las acciones del usuario y como las va a tratar el sistema, sí se almacenan en base de datos, en la nube o se modifica la interfaz de usuario.
\paragraph{Flujo del módulo de conversión y carga de archivos}
El diagrama de la Figura \ref{fig:diagrama_1} muestra el flujo del sistema al convertir y almacenar un archivo, ya sea un documento o un enlace, ilustra el flujo desde que el usuario ingresa hasta que el archivo queda registrado en la base de datos y ya visible en el módulo de gestión.
\begin{figure}[!tbh]
    \centering
    \includegraphics[width=0.9\linewidth]{img/diagrama_conversion.png}
    \caption{Diagrama de flujo: Módulo de conversión y carga de archivos. Fuente: elaboración propia}
    \label{fig:diagrama_1}
\end{figure}

\paragraph{Flujo del módulo de gestión documental}
La Figura \ref{fig:diagrama_2} muestra el flujo de este módulo, en el cual se pueden gestionar todos los archivos almacenados en el sistema, en la lista solo se muestran los archivos que no han sido eliminados y como se ve en la figura, por cada archivo se pueden hacer cuatro acciones, entre ellas la creación de códigos QR para cada archivo, los cuales tambien se guardan en la base de datos con las relaciones de su archivo.
\begin{figure}[!tbh]
    \centering
    \includegraphics[width=0.9\linewidth]{img/diagrama_gestion.png}
    \caption{Diagrama de flujo: Módulo de gestión documental. Fuente: elaboración propia}
    \label{fig:diagrama_2}
\end{figure}

\subsubsection{Diagramas de actividades}
Para ilustrar el comportamiento del sistema ante cada acción del usuario, es necesario mostrar los diagramas de actividades de los principales flujos de los procesos del sistema.

\paragraph{Proceso de conversión de documento y carga de imágen} La Figura \ref{fig:actividad-conversion} muestra el comportamiento del sistema ante distintas acciones del usuario al momento de querer converir un archivo, y se pueden observar las tecnologias que se van a utilizar en cada fase del proceso
\begin{figure}[!tbh]
    \centering
    \includegraphics[width=0.7\linewidth]{img/diagrama-actividad-conversion-.png}
    \caption{Diagrama de actividades: conversión de documento y carga de imagen. Fuente: elaboración propia}
    \label{fig:actividad-conversion}
\end{figure}

\paragraph{Proceso de registro de URL} El sistema actúa de distinta manera cuando se trata de una URL, el formulario es distinto y la forma en que se procesan los datos también lo es. Como se muestra en la Figura \ref{fig:actividad-url} el sistema valida que haya una sesión activa y despues guarda la URL para ser convertida a QR cuando se necesite.
\begin{figure}[!tbh]
    \centering
    \includegraphics[width=0.7\linewidth]{img/diagrama-actividad-url.png}
    \caption{Diagrama de actividades: registro de URL. Fuente: elaboración propia}
    \label{fig:actividad-url}
\end{figure}

\paragraph{Proceso Gestión de archivos} Finalmente, otro proceso fundamental en este sistema es el módulo de gestión documental, representado en la Figura \ref{fig:actividad-gestion} en el cual se reallizan las acciones disponibles de los archivos, como la visualización o descarga de archivos desde AWS S3, la eliminación del archivo, aquí solo se cambia el estado del archivo en la base de datos, para tener un registro total de los archivos, y por último la funcionalidad de generar códigos QR para cada archivo o enlace activo en el sistema, los cuales también se guardan en la base de datos en sus tablas correspondientes.
\begin{figure}[!tbh]
    \centering
    \includegraphics[width=0.9\linewidth]{img/diagrama-actividad-gestion.png}
    \caption{Diagrama de actividades: gestión de archivos. Fuente: elaboración propia}
    \label{fig:actividad-gestion}
\end{figure}

\subsection{Diseño del sistema}
\label{diseño_sistema}

\subsubsection{Diagrama de Entidad-Relación de la Base de Datos}
El diagrama de entidad-relación de la Figura \ref{fig:modelo-er} corresponde al modelo utilizado para este sistema, la base de datos en SQL Server, consta de cinco entidades y sus relaciones entre sí. Todos los identificadores, que también son las llaves primarias, utilizan el tipo de dato ULID para mayor seguridad y para garantizar que el identificador sea único.

La entidad Area representa las áreas en las que se divide la institución, y a las cuales estan relacionados cada usuario o empleado, y asi organizar los archivos por área para facilitar su gestión posteriormente. Un Area puede tener múltiples usuarios y un usuario puede estar relacionado a múltiples archivos, la entidad Archivo registra todos los metadatos de los archivos subidos al sistema.

La entidad Usuario también se relaciona con la entidad Log\_acceso la cual se encarga de registrar todas las acciones realizadas por un usuario en el sistema.
\begin{figure}[!tbh]
    \centering
    \includegraphics[width=0.7\linewidth]{img/modelo ER.png}
    \caption{Modelo Entidad-Relación del sistema. Fuente: elaboración propia}
    \label{fig:modelo-er}
\end{figure}

\subsubsection{Diagramas de secuencia}

\paragraph{Proceso de Autenticación vía AuthToken}
La Figura \ref{fig:sec-autenticacion} es el diagrama de secuencia del proceso de autenticación del sistema, este proceso inicia cuando el usuario accede al sistema mediante un enlace ya predeterminado por la institución, que contiene el parámetro AuthToken, el cual contiene el nombre de usuario y la contraseña. Los archivos del sistema que interactuan son el archivo AuthMiddleware el cual detiene la petición y evalúa si existe una sesión activa o no, si no existe un sesión se ejecuta el archivo AuthService el cual se encarga de consultar la base de datos para verificar si ya existe un usuario con ese nombre para actualizar su último acceso, si no existe un usuario crea el área con el nombre de usuario y los vincula.
Una vez identificado el usuario, el Middleware guarda su identificador, nombre y área en la sesión del navegador, para que el sistema reconozca al usuario en todas las pantallas posteriores sin necesidad de repetir el proceso.
\begin{figure}[!tbh]
    \centering
    \includegraphics[width=0.7\linewidth]{img/secuencia-autenticacion.png}
    \caption{Diagrama de secuencia: Auntenticación vía AuthToken. Fuente: elaboración propia}
    \label{fig:sec-autenticacion}
\end{figure}

\paragraph{Proceso de Conversion y Generación de Código QR}
En la Figura \ref{fig:sec-convertir} se observa el diagrama de secuencia del módulo de conversión y carga de archivos, en el cual hay tres opciones posibles para convertir y almacenar, el sistema evalúa que tipo de archivo es y depende de eso ocurre un flujo de procesos distintos, tanto en la imagen como en el documento el sistema sube los archivos a AWS S3 pero en la URL solo se registra en la base de datos. Después del proceso de conversion, se observa el proceso de la creación de códigos QR, en el cual el archivo QrService obtiene los datos del archivo, genera el código y posteriormente lo sube a S3 y en la base de datos establece la relación del código con el archivo al que pertence. Al final del diagrama esta el proceso ejecutado por el archivo LogService, el cual registra toda acción de los usuarios en su tabla correspondiente de la BD, esto para mayor seguridad y transparencia con el manejo de información, por último el sistema muestra la vista correspondiente al usuario.
\begin{figure}[!tbh]
    \centering
    \includegraphics[width=0.9\linewidth]{img/secuencia-convertir.png}
    \caption{Diagrama de secuencia: Conversion y Generación de Códigos QR. Fuente: elaboración propia}
    \label{fig:sec-convertir}
\end{figure}



\subsubsection{Diseño de interfaz}
Para la propuesta del sistema se realizaron dos vistas principales para dividir las funcionalidades de los módulos más del sistema.

\paragraph{Modulo de conversion y carga de archivos}
\begin{figure}[!tbh]
    \centering
    \includegraphics[width=0.7\linewidth]{img/Mockup-Modulo de conversion y carga de archivos.png}
    \caption{Prototipo de la interfaz del módulo de conversion y carga de archivos. Fuente: elaboración propia}
    \label{fig:mockup-1}
\end{figure}
El diseño del módulo de conversión y carga de archivos se muestra en la Figura \ref{fig:mockup-1}. La pantalla se divide en dos secciones diferentes con su formulario, uno para carga de documentos o imágenes, en el cual debe de seleccionar el tipo de documento, para facilitar la búsqueda de documentos en el módulo de gestión y el otro para registrar las URLs.
Ambas secciones comparten la misma página para agrupar en un solo lugar todas las formas para subir archivos al sistema.

\paragraph{Modulo de gestión documental}
\begin{figure}[!tbh]
    \centering
    \includegraphics[width=0.7\linewidth]{img/Mockup-Modulo de gestión documental.png}
    \caption{Prototipo de la interfaz del módulo de gestión documental. Fuente: elaboración propia}
    \label{fig:mockup-2}
\end{figure}
El otro módulo del sistema es el de gestión documental, cuyo diseño se ilustra en la Figura \ref{fig:mockup-2} , la pantalla muestra una tabla con todos los archivos registrados en el sistema, incluyendo el nombre, tipo de documento, área que subió el archivo y fecha en el que lo subieron. En la parte superior izquierda se incluye un filtro de búsqueda para encontrar archivos más facilmente por tipo o área y una barra de búsqueda para rastrear un archivo por nombre.


\subsection{Desarrollo del sistema}
herramientas, lenguajes de programación y la arquitectura para construir los módulos